<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="urlcatcher.girim.com.girimproxy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="*.wikipedia.org" />
                <data android:scheme="https" android:host="*.wikipedia.org" />
                <data android:scheme="http" android:host="*.*.wikipedia.org" />
                <data android:scheme="https" android:host="*.*.wikipedia.org" />
                <data android:scheme="http" android:host="*.wikimedia.org" />
                <data android:scheme="https" android:host="*.wikimedia.org" />
                <data android:scheme="http" android:host="*.*.wikimedia.org" />
                <data android:scheme="https" android:host="*.*.wikimedia.org" />
                <data android:scheme="http" android:host="*.imgur.com" />
                <data android:scheme="https" android:host="*.imgur.com" />
            </intent-filter>
        </activity>
    </application>

</manifest>